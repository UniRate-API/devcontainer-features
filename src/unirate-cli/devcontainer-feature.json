{
    "name": "UniRate CLI",
    "id": "unirate-cli",
    "version": "1.0.0",
    "description": "Installs the unirate CLI — currency exchange rates, conversion, and VAT lookups from the UniRate API. A single zero-dependency static Go binary, downloaded from the official GitHub release and verified against the published SHA-256 checksums.",
    "options": {
        "version": {
            "type": "string",
            "default": "latest",
            "description": "Version of the unirate CLI to install: a release tag such as '0.1.0', or 'latest'."
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils"
    ],
    "documentationURL": "https://github.com/UniRate-API/devcontainer-features/tree/main/src/unirate-cli",
    "licenseURL": "https://github.com/UniRate-API/devcontainer-features/blob/main/LICENSE",
    "keywords": [
        "unirate",
        "currency",
        "exchange-rates",
        "forex",
        "vat",
        "cli"
    ]
}
